# .travis.yml
#
# parrot

language: rust
cache:
  cargo: true
rust:
  - nightly
env:
  global:
    - CLIPPY_TOOLCHAIN=nightly
script:
  - cargo build --verbose --all
  - cargo test --verbose --all
branches:
  only:
    - master
